info:
  name: Get Session
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/auth/session"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
